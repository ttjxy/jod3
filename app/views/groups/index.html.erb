

<table class="table table-bordered">


  <% @groups.each do |group| %>
  <tr>
    <td>
      <%= link_to(group.title, group_path(group)) %>
    </td>

    <td>
      <%= group.created_at %>
    </td>
  </tr>
  <% end %>
</table>
